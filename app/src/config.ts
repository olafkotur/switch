// @ts-ignore
import { version } from '../../package.json'

export const config = {
  appVersion: version,
  projectRepo: 'https://github.com/olafkotur/switch',
  apiUrl: 'http://localhost:8080',
  defaultError: 'Unexpected error occurred',
}
